# Multi-Category-Type System Enhancement

## Goal
Transform the single "Category" dropdown per entity into a flexible multi-category-type system. Hotels can have separate dropdowns for "Category" (Luxury/Budget), "Star Rating" (3/4/5), "Style" (Beach/City), etc. Each type has a customizable multilingual label. New types auto-appear as new dropdowns in the add/edit form.

---

## Part 1: Database + Backend (Completed)

---

## Part 2: Nav Dropdown Category Types + Public Filtering

### Goal
When configuring navigation dropdowns in site settings, add a new mode "Category Types" that lets admins select a category type from the new `category_types` table. The dropdown then shows that type's values. On public pages, users can filter by one or more category values from any type.

---

## 2A. Reorganize dropdown modes to 3 options

Simplify to exactly 3 modes in the site settings nav dropdown:

### Mode 1: Dynamic Categories (`categories`)
- Admin selects a **category type** from the new `category_types` table
- The navbar shows ALL values of that type as dropdown links
- Each value links to the page with `?category_{type_key}={value_key}` filter

### Mode 2: Filter (`filter`)
- Admin selects a **single category value** from ALL values across ALL types for that entity
- The navbar shows that single item as a link
- Links to page with `?category_{type_key}={value_key}` filter

### Mode 3: Search (`search`)
- Stays as is - admin types a search keyword

### `resources/js/lib/nav-config.ts`
- Remove `'category-types'` from `DropdownItemMode` (revert to `'filter' | 'search' | 'categories'`)
- Remove `categoryTypeKey` field from `DropdownItemConfig` (no longer needed - use `value` to store type key for categories mode)
- Update `buildItemHref()` to handle categories mode with type key

### `resources/js/pages/admin/site-settings/AdminSiteSettingsNav.tsx`
- Remove the `category-types` mode option from the `<Select>`
- Keep only: `filter`, `categories`, `search`
- For `categories` mode:
  - Show a `<Select>` to pick a category TYPE (from `categoryTypes`)
  - Store the type key in `item.value`
  - The label auto-updates to the type's label
- For `filter` mode:
  - Show a `<Select>` with ALL values from ALL category types combined
  - Each option shows: `{type_label} > {value_name}` format (e.g., "Category > Luxury")
  - Store as `{typeKey}:{valueKey}` in `item.value`
  - The label auto-updates to the value's name

### `resources/js/components/layout/Navbar.tsx`
- Update `resolveDropdownItems()` for `categories` mode:
  - When mode = `categories`, look up the category type by `item.value` (which stores the type key)
  - Expand into filter items for each value of that type
  - Each expanded item gets `categoryTypeKey` set to the type key
- Update `buildItemHref()` to handle filter items with `categoryTypeKey`

---

## 2B. Public listing pages - multi-select category type filtering

### `resources/js/pages/hotels/index.tsx`
- Read URL params for category type filters (e.g., `?category_type_star-rating=5-stars&category_type_style=beach`)
- Add filter sections for each active category type
- Each category type shows its values as multi-select pills/buttons
- Combine filters with AND logic (entity must match ALL selected category types) and OR within each type

### `resources/js/pages/tours/index.tsx`
- Same pattern: read URL params, show category type filters, multi-select

### `resources/js/pages/destinations/index.tsx`
- Same pattern

### `resources/js/pages/events/index.tsx`
- Same pattern

### `resources/js/pages/deals/index.tsx`
- Same pattern

### `resources/js/pages/cars/index.tsx`
- Same pattern

### `resources/js/pages/blog/` or `resources/js/pages/admin/AdminBlog.tsx` public page
- Same pattern

### Shared filter component
Create `resources/js/components/lists/CategoryTypeFilter.tsx`:
- Props: `categoryTypes: CategoryType[]`, `selectedFilters: Record<string, string[]>`, `onFilterChange: (typeKey: string, values: string[]) => void`, `lang: string`
- Renders a section per category type with multi-select pill buttons
- Each pill shows the value name in the active language
- Selecting a pill adds/removes it from the filter

### `resources/js/hooks/usePublicData.ts`
- Update `useCategoryTypesPublic()` to be usable from listing pages
- The hook already exists and returns category types with values

### Public API changes
No backend API changes needed - the public pages already load all entities client-side and filter in-browser. The category type data comes from the existing `/api/categories/types` endpoint.

---

## 2C. URL param format

Use URL search params like:
```
/hotels?category_star-rating=5-stars,4-stars&category_style=beach,city
```

Format: `category_{type_key}={value_key1},{value_key2}`

This allows:
- Multiple values per type (comma-separated) = OR within type
- Multiple types in same URL = AND across types

### Filter mode URL format
When a filter item links to a page, the URL is:
```
/hotels?category_{typeKey}={valueKey}
```

### Categories mode URL format
When a categories dropdown item expands into values, each value links to:
```
/hotels?category_{typeKey}={valueKey}
```

---

## Files to modify

### New files
| File | Purpose |
|------|---------|
| `resources/js/components/lists/CategoryTypeFilter.tsx` | Reusable multi-select filter component for category types (already created) |

### Modified files
| File | Changes |
|------|---------|
| `resources/js/lib/nav-config.ts` | Simplify modes to 3, remove categoryTypeKey field |
| `resources/js/pages/admin/site-settings/AdminSiteSettingsNav.tsx` | Simplify mode selector, update filter/categories UI |
| `resources/js/components/layout/Navbar.tsx` | Update resolveDropdownItems for categories mode |
| `resources/js/pages/hotels/index.tsx` | Category type filters (already done) |
| `resources/js/pages/tours/index.tsx` | Same (already done) |
| `resources/js/pages/destinations/index.tsx` | Same (already done) |
| `resources/js/pages/events/index.tsx` | Same (already done) |
| `resources/js/pages/deals/index.tsx` | Same (already done) |
| `resources/js/pages/cars/index.tsx` | Same (already done) |
| `resources/js/i18n/translations.ts` | Update translations for simplified modes |

---

## Part 3: Fix Backend + Implement Filtering + Unique Filter Styles

### Problem
1. Public API controllers don't return `category_assignments` → frontend filtering has no data
2. All listing pages use the same generic CategoryTypeFilter component → no visual variety
3. Navbar dropdown clicks link to the page but filtering may not activate properly

---

## 3A. Fix Public API Controllers

Each public controller must eager-load `categoryAssignments` and include `category_assignments` in the response.

### `app/Http/Controllers/Api/HotelController.php`
- Eager-load `categoryAssignments.categoryType` and `categoryAssignments.categoryValue` in queries
- Add `category_assignments` to `payload()` method (same pattern as AdminHotelController)

### `app/Http/Controllers/Api/TourController.php`
- Same: eager-load relationships, add to payload

### `app/Http/Controllers/Api/DestinationController.php`
- Same

### `app/Http/Controllers/Api/EventController.php`
- Same

### `app/Http/Controllers/Api/DealController.php`
- Same

### `app/Http/Controllers/Api/CarController.php`
- Same

### Helper pattern for each controller:
```php
// In the query chain, add:
->with(['categoryAssignments.categoryType', 'categoryAssignments.categoryValue'])

// In the payload method, add:
'category_assignments' => collect($item->categoryAssignments ?? [])->mapWithKeys(
    fn($a) => [$a->categoryType->key => $a->categoryValue->key]
)->toArray(),
```

---

## 3B. Update TypeScript Types

### `resources/js/api/entities.api.ts` (or wherever public entity types are defined)
Add `category_assignments?: Record<string, string>` to each entity type:
- `HotelItem`
- `TourItem`
- `DestinationItem`
- `EventItem`
- `DealItem`
- `CarItem`

This eliminates the `as unknown as Record<string, unknown>` cast hack.

---

## 3C. Unique Filter Styles Per Page

Each listing page gets its own filter section style instead of using the generic CategoryTypeFilter component everywhere.

### `resources/js/pages/hotels/index.tsx` — Sidebar accordion style
- Category type filters in the sticky sidebar
- Each type is a collapsible section with a header showing the type label
- Values are rendered as colored pill badges (reuse existing TagFilter-like style)
- Already has star filter as pill buttons — category types follow the same pattern

### `resources/js/pages/tours/index.tsx` — Horizontal pill bar
- Category type filters inside the ListFilterBar
- Each type is a row of rounded-full pill buttons (like the existing category buttons)
- Active pill gets primary fill, inactive gets outline

### `resources/js/pages/destinations/index.tsx` — Inline button group
- Category type filters in the top row alongside search and country
- Each type is a row of rounded-xl buttons
- Similar to existing category button style but distinct with different border radius

### `resources/js/pages/events/index.tsx` — Tag grid
- Category type filters inside ListFilterBar
- Each type rendered as a compact tag grid (smaller pills)
- Different color scheme per type

### `resources/js/pages/deals/index.tsx` — Compact filter chips
- Category type filters inside ListFilterBar
- Small chip-style filters with checkmark icons when active

### `resources/js/pages/cars/index.tsx` — Select dropdown grid
- Category type filters as `<Select>` dropdowns in the filter area
- Each type gets its own dropdown in the existing 4-column grid layout

### Approach
Instead of modifying the shared `CategoryTypeFilter.tsx`, each page will inline its own filter rendering for category types. This gives each page a unique look while sharing the same data and state pattern.

---

## 3D. Fix Filter Predicate in All Pages

Update the filter logic to use the properly typed `category_assignments` field instead of casting to `Record<string, unknown>`.

### Pattern for each page:
```typescript
const matchesCategoryTypes = Object.entries(categoryTypeFilters).every(
    ([typeKey, values]) =>
        values.length === 0 ||
        (hotel.category_assignments && values.includes(hotel.category_assignments[typeKey])),
);
```

Remove the `as unknown as Record<string, unknown>` cast.

---

## 3E. Fix Navbar Dropdown Click → URL Filtering

The navbar already builds correct URLs via `buildItemHref()` with `?category_{typeKey}={valueKey}` format. The listing pages already parse these params. Verify the flow works end-to-end.

---

## Files to modify

### Backend (6 files)
| File | Change |
|------|--------|
| `app/Http/Controllers/Api/HotelController.php` | Eager-load + add category_assignments to payload |
| `app/Http/Controllers/Api/TourController.php` | Same |
| `app/Http/Controllers/Api/DestinationController.php` | Same |
| `app/Http/Controllers/Api/EventController.php` | Same |
| `app/Http/Controllers/Api/DealController.php` | Same |
| `app/Http/Controllers/Api/CarController.php` | Same |

### Frontend types (1 file)
| File | Change |
|------|--------|
| `resources/js/api/entities.api.ts` | Add category_assignments to entity types |

### Frontend listing pages (6 files)
| File | Change |
|------|--------|
| `resources/js/pages/hotels/index.tsx` | Inline category type filters in sidebar style, fix predicate |
| `resources/js/pages/tours/index.tsx` | Inline category type filters in pill bar style, fix predicate |
| `resources/js/pages/destinations/index.tsx` | Inline category type filters in button group style, fix predicate |
| `resources/js/pages/events/index.tsx` | Inline category type filters in tag grid style, fix predicate |
| `resources/js/pages/deals/index.tsx` | Inline category type filters in chip style, fix predicate |
| `resources/js/pages/cars/index.tsx` | Inline category type filters in select dropdown style, fix predicate |

---

## Part 4: Configurable Filter Styles with Admin Preview

### Goal
Each category type gets a configurable filter style (pill buttons, checkbox list, dropdown, slider, radio group, toggles, color swatches). Admin configures the style per type with a live preview. Public pages render filters using the configured style.

---

## 4A. Database Migration

### New column on `category_types`: `filter_style`

```php
Schema::table('category_types', function (Blueprint $table) {
    $table->string('filter_style')->default('pills')->after('sort_order');
});
```

Valid values: `pills`, `checkbox`, `dropdown`, `slider`, `radio`, `toggles`, `colors`

---

## 4B. Backend Changes

### `app/Models/CategoryType.php`
- Add `filter_style` to `$fillable`

### `app/Http/Controllers/Api/AdminCategoryTypeController.php`
- Add `filter_style` to validation rules in `store()` and `update()`
- Accept `filter_style` in request and save it

### `app/Http/Controllers/Api/AdminCategoryController.php` (typesByEntity endpoint)
- Include `filter_style` in the nested response

---

## 4C. Frontend Types

### `resources/js/hooks/usePublicData.ts`
- Add `filter_style` to `PublicCategoryType`

### `resources/js/api/categoryTypes.api.ts`
- Add `filter_style` to `CategoryType` interface

---

## 4D. New Filter Components

Create 7 filter components in `resources/js/components/filters/`:

### `PillFilter.tsx` (existing pattern, extract to component)
- Rounded pill buttons, multi-select
- Active: `bg-primary text-primary-foreground`
- Inactive: `border border-border text-muted-foreground`

### `CheckboxFilter.tsx`
- Vertical list of checkboxes with labels
- Uses existing `Checkbox` component from `@/components/ui/checkbox`
- Shows count of selected items

### `DropdownFilter.tsx`
- Multi-select dropdown using existing `Select` component
- Shows selected count as badge

### `SliderFilter.tsx`
- Range slider using existing `Slider` component
- For numeric values (e.g., star ratings 1-5)
- Shows min/max labels and current value

### `RadioFilter.tsx`
- Single-select radio group using existing `RadioGroup` component
- Only one value can be active at a time

### `ToggleFilter.tsx`
- Toggle switches using existing `Switch` component
- Each value gets its own toggle row

### `ColorFilter.tsx`
- Color circle buttons
- Each value has a `color` field (stored as hex in category_type_values)
- Active: ring/border highlight

### `FilterRenderer.tsx` (orchestrator)
- Takes `categoryType` and `selectedValues` and `onChange`
- Renders the appropriate component based on `categoryType.filter_style`
- Used by all public listing pages

---

## 4E. CategoryTypeManager Enhancement

### `resources/js/components/admin/CategoryTypeManager.tsx`

Add filter style selector to the type editing form:

1. When creating/editing a category type, show a `<Select>` for filter_style
2. Below the selector, show a **live preview** of how the filter will look
3. The preview renders the actual filter component with sample data

Preview section:
```tsx
<div className="mt-4 rounded-lg border border-dashed p-4">
    <Label className="text-xs text-muted-foreground">Preview</Label>
    <div className="mt-2">
        <FilterRenderer
            categoryType={{ ...newTypeLabel, values: sampleValues, filter_style: selectedStyle }}
            selectedValues={[]}
            onChange={() => {}}
            lang={lang}
            preview
        />
    </div>
</div>
```

Sample values are generated from the actual values of the type (if editing) or placeholder values (if creating).

---

## 4F. Admin Category Type Config Page

### `resources/js/components/admin/CategoryTypeManager.tsx` updates

The type creation/editing form now has:

1. **Label** (en/fr/ar) - existing
2. **Filter Style** - new `<Select>` with options:
   - Pill Buttons (pills)
   - Checkbox List (checkbox)
   - Dropdown (dropdown)
   - Slider (slider)
   - Radio Group (radio)
   - Toggle Switches (toggles)
   - Color Swatches (colors)
3. **Live Preview** - renders the selected style with sample data
4. **Values** - existing CRUD

For `colors` style, each value needs a `color` hex field. Add a color picker input to the value form when the type's filter_style is `colors`.

---

## 4G. Color Value Support

### `category_type_values` table migration
```php
Schema::table('category_type_values', function (Blueprint $table) {
    $table->string('color', 7)->nullable()->after('name');
});
```

### `app/Models/CategoryValue.php`
- Add `color` to `$fillable`

### `resources/js/api/categoryTypes.api.ts`
- Add `color?: string` to `CategoryTypeValue`

### Admin value form
- When type's filter_style is `colors`, show a color picker input for each value

---

## 4H. Public Listing Pages Update

Each listing page replaces the inline filter code with:

```tsx
import { FilterRenderer } from '@/components/filters/FilterRenderer';

// In the filter section:
{categoryTypes.map((catType) => (
    <FilterRenderer
        key={catType.key}
        categoryType={catType}
        selectedValues={categoryTypeFilters[catType.key] ?? []}
        onChange={(values) =>
            setCategoryTypeFilters((prev) => ({ ...prev, [catType.key]: values }))
        }
        lang={lang}
    />
))}
```

This replaces ALL the per-page inline filter code with a single shared component.

---

## Files to create

| File | Purpose |
|------|---------|
| `resources/js/components/filters/FilterRenderer.tsx` | Orchestrator - picks correct filter component |
| `resources/js/components/filters/PillFilter.tsx` | Pill button filter |
| `resources/js/components/filters/CheckboxFilter.tsx` | Checkbox list filter |
| `resources/js/components/filters/DropdownFilter.tsx` | Dropdown select filter |
| `resources/js/components/filters/SliderFilter.tsx` | Range slider filter |
| `resources/js/components/filters/RadioFilter.tsx` | Radio group filter |
| `resources/js/components/filters/ToggleFilter.tsx` | Toggle switch filter |
| `resources/js/components/filters/ColorFilter.tsx` | Color swatch filter |

## Files to modify

| File | Changes |
|------|---------|
| `database/migrations/2026_07_02_000005_add_filter_style_to_category_types.php` | New migration |
| `database/migrations/2026_07_02_000006_add_color_to_category_type_values.php` | New migration |
| `app/Models/CategoryType.php` | Add filter_style to fillable |
| `app/Models/CategoryValue.php` | Add color to fillable |
| `app/Http/Controllers/Api/AdminCategoryTypeController.php` | Accept filter_style |
| `app/Http/Controllers/Api/AdminCategoryController.php` | Include filter_style in response |
| `resources/js/api/categoryTypes.api.ts` | Add filter_style and color fields |
| `resources/js/hooks/usePublicData.ts` | Add filter_style to PublicCategoryType |
| `resources/js/components/admin/CategoryTypeManager.tsx` | Add style selector + preview |
| `resources/js/pages/hotels/index.tsx` | Use FilterRenderer |
| `resources/js/pages/tours/index.tsx` | Use FilterRenderer |
| `resources/js/pages/destinations/index.tsx` | Use FilterRenderer |
| `resources/js/pages/events/index.tsx` | Use FilterRenderer |
| `resources/js/pages/deals/index.tsx` | Use FilterRenderer |
| `resources/js/pages/cars/index.tsx` | Use FilterRenderer |

---

## Verification

1. **Admin - Create type with style**: Create a category type with "Slider" style → verify filter_style is saved
2. **Admin - Preview**: When selecting "Checkbox" style, verify the preview shows checkboxes
3. **Admin - Color values**: Create a "Colors" type → add values with color hex → verify color picker works
4. **Public - Pills**: Set type to "Pills" → verify public page shows pill buttons
5. **Public - Checkbox**: Set type to "Checkbox" → verify public page shows checkbox list
6. **Public - Dropdown**: Set type to "Dropdown" → verify public page shows multi-select dropdown
7. **Public - Slider**: Set type to "Slider" → verify public page shows range slider
8. **Public - Radio**: Set type to "Radio" → verify public page shows radio buttons
9. **Public - Toggles**: Set type to "Toggles" → verify public page shows toggle switches
10. **Public - Colors**: Set type to "Colors" → verify public page shows color swatches
11. **Filtering works**: Select values in any filter style → verify entities are filtered correctly
12. **URL persistence**: Copy filtered URL → open in new tab → verify filters restored
13. **RTL**: Verify all filter styles work in Arabic layout

---

## Database Changes

### New table: `category_types`
Defines what category types exist per entity.

| Column | Type | Notes |
|--------|------|-------|
| id | bigint PK | |
| entity_type | string, indexed | 'hotels', 'tours', etc. |
| key | string, indexed | auto-slug from English label |
| label | JSON | `{en, fr, ar}` — the dropdown label |
| sort_order | smallint, default 0 | |
| timestamps | | |
| UNIQUE(entity_type, key) | | |

### New table: `category_type_values`
The actual category options within each type.

| Column | Type | Notes |
|--------|------|-------|
| id | bigint PK | |
| category_type_id | FK → category_types | cascade delete |
| key | string, indexed | auto-slug from English name |
| name | JSON | `{en, fr, ar}` |
| timestamps | | |
| UNIQUE(category_type_id, key) | | |

### New table: `entity_category_assignments`
Pivot linking entities to their selected category values.

| Column | Type | Notes |
|--------|------|-------|
| id | bigint PK | |
| entity_type | string, indexed | 'hotels', 'tours', etc. |
| entity_id | bigint, indexed | |
| category_type_id | FK → category_types | cascade delete |
| category_value_id | FK → category_type_values | cascade delete |
| UNIQUE(entity_type, entity_id, category_type_id) | | one value per type per entity |

### Data migration seeder
1. For each `entity_type` in existing `categories`, create a `CategoryType` with label "Category"/"Catégorie"/"فئة"
2. Migrate each existing category row → `CategoryValue` linked to that type
3. For each entity with a `category_key` → create `EntityCategoryAssignment`

---

## Backend Changes

### New Models
- `app/Models/CategoryType.php` — hasMany values, hasMany assignments
- `app/Models/CategoryValue.php` — belongsTo type
- `app/Models/EntityCategoryAssignment.php` — belongsTo type + value

### New Controller: `app/Http/Controllers/Api/AdminCategoryTypeController.php`
CRUD for category types and their values:

| Route | Action |
|-------|--------|
| `GET /api/admin/category-types?type=hotels` | List types for entity |
| `POST /api/admin/category-types` | Create type (auto-slug key from label.en) |
| `PUT /api/admin/category-types/{type}` | Update type label |
| `DELETE /api/admin/category-types/{type}` | Delete type (409 if used by entities) |
| `GET /api/admin/category-types/{type}/values` | List values |
| `POST /api/admin/category-types/{type}/values` | Add value |
| `PUT /api/admin/category-types/{type}/values/{value}` | Update value name |
| `DELETE /api/admin/category-types/{type}/values/{value}` | Delete value (409 if used) |

### Update `AdminCategoryController.php`
Add nested endpoint: `GET /api/admin/categories/types?entity_type=hotels` — returns types with their values for public use.

### Update Entity Controllers (7 controllers)
Each admin entity controller needs:
1. Accept `category_assignments: { type_key: value_key, ... }` in store/update
2. After save, call `syncCategoryAssignments()` — deletes old rows, creates new ones
3. Keep legacy `category_key`/`category` columns synced from first assignment
4. In `adminPayload()`, eager-load assignments and include `category_assignments` in output

**Affected controllers:**
- `AdminHotelController.php`
- `AdminTourController.php`
- `AdminCarController.php`
- `AdminEventController.php`
- `AdminDealController.php`
- `AdminBlogPostController.php`
- `AdminDestinationController.php`

### Update Public Controllers (7 controllers)
Include `category_assignments` in public API responses alongside existing fields.

### Update `routes/api.php`
Add category-types routes inside admin middleware group.

---

## Frontend Changes

### New Files

| File | Purpose |
|------|---------|
| `resources/js/api/categoryTypes.api.ts` | API functions + TypeScript types for category types/values |
| `resources/js/hooks/useCategoryTypes.ts` | React Query hook: `useCategoryTypes(entityType)` |
| `resources/js/components/admin/CategoryTypeManager.tsx` | Two-level management dialog (types → values) |

### Rewrite: `CategoryTypeManager.tsx` (replaces CategoryManager.tsx)
Two-level management dialog:
- **Level 1**: List all types for the entity (label in 3 langs, edit/delete, add new)
- **Level 2**: Click a type → manage its values (name in 3 langs, add/delete)
- Back button returns to types list

### Update All Admin Pages (7 pages)
Each admin page follows this pattern:

1. Replace `fetchCategories(type)` with `useCategoryTypes(entityType)`
2. In form `render()`, iterate `categoryTypes` and render a `<Select>` per type:
   - Label = `catType.label[activeLang]` (multilingual dropdown name)
   - Options = `catType.values` (the category options)
   - Value stored as `values['category_' + catType.key]`
3. Update `handleSave()` to send `category_assignments: { [typeKey]: valueKey }`
4. Update form initialization to load existing assignments
5. Replace `<CategoryManager type="hotels">` with `<CategoryTypeManager entityType="hotels">`

**Affected pages:**
- `AdminHotels.tsx`, `AdminTours.tsx`, `AdminCars.tsx`, `AdminEvents.tsx`
- `AdminDeals.tsx`, `AdminBlog.tsx`, `AdminDestinations.tsx`

### Update Public Pages
- Update `useCategories` hook to fetch from new nested endpoint
- Keep filter tabs working using primary category type
- Entity cards show category type badges

### Update Utility Files
- `categoryCache.ts` — extend cache key for category-types
- `categoryLabels.ts` — add helpers for nested resolution
- Keep existing functions for backwards compatibility

---

## Implementation Order

### Phase 1: Database + Models (no breaking changes)
1. Create 3 new migrations (category_types, category_type_values, entity_category_assignments)
2. Create 3 new models
3. Create data migration seeder
4. All existing functionality continues working

### Phase 2: Backend Controller
1. Create `AdminCategoryTypeController` with full CRUD
2. Add routes to `api.php`
3. Add nested types endpoint to `AdminCategoryController`

### Phase 3: Entity Controller Updates
1. Update each admin entity controller to accept `category_assignments`
2. Add `syncCategoryAssignments()` helper
3. Update `adminPayload()` to include assignments
4. Update public controllers

### Phase 4: Frontend API + Hook
1. Create `categoryTypes.api.ts`
2. Create `useCategoryTypes` hook

### Phase 5: Frontend Components
1. Create `CategoryTypeManager.tsx`
2. Update all 7 admin pages to use multi-dropdown pattern

### Phase 6: Public Frontend
1. Update `useCategories` hook
2. Update listing page filters
3. Update entity cards to show category type labels

---

## Verification

1. **Admin forms**: Create hotel with 3 category types → all dropdowns appear, selection saved correctly
2. **CategoryTypeManager**: Create new type → dropdown appears in admin form without page refresh
3. **CategoryTypeManager**: Add value to type → appears in dropdown options
4. **Edit flow**: Edit hotel → dropdowns show previously selected values
5. **Delete safety**: Delete type used by entities → 409 with affected items list
6. **Public pages**: Filter tabs still work (backwards compatible)
7. **Multi-language**: Switch language → type labels and value names update
8. **RTL**: Arabic layout works for all new UI elements
9. **Legacy sync**: `category_key`/`category` columns stay populated on entity tables
10. **Migration**: All existing categories migrated to new structure with no data loss
