# Plan: Add special promos button to Navbar

## Files to modify
1. `resources/js/components/layout/Navbar.tsx` — add blue animated button left of LanguageSwitcher
2. `resources/js/pages/promos/index.tsx` — add `is_special` filter support via URL param

## Changes

### 1. Navbar.tsx — Add special promos button
- Add a blue pulsing/glowing button with a star icon
- Position it to the left of `<LanguageSwitcher />` in the desktop nav (line 1034)
- Link to `/promos?special=true`
- Add the same button in the mobile menu
- Use framer-motion for subtle animation (pulse glow effect)

### 2. promos/index.tsx — Support `special` URL param
- Read `special` from search params: `params.get('special')`
- Filter promos where `promo.is_special === true` when param is present
- Pre-set the filter state accordingly

## Verification
- Click the blue button → navigates to `/promos?special=true`
- Promos page shows only special promos when param is present
- Button is visible on both desktop and mobile
