- the user wants the search widget to appear partially overlapping the bottom edge of the PageHeroCarousel (placed on top of the last 20px of the carousel at the bottom)
- this means the search widget PLUS the hero must become carrot sm=ch hero OR the search must be moved into the hero/carrot section.
- the user says they want the search to start at the last 20px of the carousel

# Approach
- Move SearchWidget from HeroSection INTO the PageHeroCarousel, positioned as an absolutely positioned element at the bottom of the carousel (overlapping the bottom 20px)
- Alternatively, position SearchWidget to overlap the hero using negative margins or absolute positioning within HeroSection
- Best approach: keep SearchWidget in HeroSection but wrap the PageHeroCarousel and SearchWidget in a shared relative container so the SearchWidget can absolutely overlap using -bottom-Y. However, since we must work with the current component arrangement (PageHeroCarousel then HeroSection sequentially), apply a negative margin to HeroSection container, or move SearchWidget into PageHeroCarousel.

# Plan 
## Analysis:
- Index.tsx layout: PageHeroCarousel -> HeroSection -> LandingSections -> ...
- HeroSection contains SearchWidget and some commented-out content
- PageHeroCarousel is 500px high and has relative layout
- HeroSection has relative layout with ``min-h-screen`` and a container with text-center

# Design:
1. In Index.tsx, wrap PageHeroCarousel and HeroSection in a shared section to allow negative/overlap positioning, OR wrap them in a div relative.
2. In HeroSection, remove the min-h-screen and excess padding, making it overlap the carousel by using relative negative top margin, or better: position SearchWidget with an absolutely-positioned wrapper (which sits naturally in the hero Section after carousel).
3. The SearchWidget itself is placed with `relative -bottom-[20px]` somewhere near bottom of PageHeroCarousel.
4. Style SearchWidget wrapper to be near/at the bottom of the carousel (or as an overlap so the top 20 px extends into the carousel). 
5. In HeroSection, the container must have ``relative z-10 mx-auto px-4 pt-20 text-center``. It is not absolute positioned. We can apply `-mt-20` or similar to make the section overlap, but the simplest approach to keep the search visually at the end of the carousel is: move the SearchWidget inside the PageHeroCarousel and position it absolutely at the bottom (inside the same relative container that has `relative w-full`)

## Implementation plan:
- In `resources/js/pages/general/Index.tsx`, remove `HeroSection`.
- In `resources/js/components/ui/SearchWidget.tsx` or better, in `PageHeroCarousel`, add an optional prop `overlaySearch?: boolean` to include SearchWidget at the bottom of the carousel.
- Modify `PageHeroCarousel.tsx`: add `import { SearchWidget } from '@/components/ui/SearchWidget';`  and render it inside the container  e.g. at the bottom with `absolute bottom-0 left-0 right-0 -translate-y-[20px]` or similar.
- Alternatively, if keeping separate: in `HeroSection.tsx` remove the min-h-screen and make it align exactly at the end of the carousel with no gap. If SearchWidget is desired "at the end (within the last 20px)" of the carousel, place the SearchWidget inside PageHeroCarousel (or wrap both in a parent in Index.tsx).

## Decision:
To have search appear at the end of the carousel (overlapping its last 20px), the best is to render it inside PageHeroCarousel at `bottom-[20px]` (inside the `relative w-full container`). This ensures the visual layout is as requested and avoids negative margins between the two components. But if the user wants the search to start at the last 20px of the carousel rather than overlap, put SearchWidget in PageHeroCarousel with `absolute bottom-[20px] left-0 right-0 z-10 px-4`

Or: keep it simple: wrap the carousel and the search widget in a parent container with relative positioning in `Index.tsx` and then position the widget.

# Recommended Plan:
1. **Edit `resources/js/pages/general/Index.tsx`:**
   - Wrap `PageHeroCarousel` and `HeroSection` in a `<div className="relative">` and apply `z-index`.
   - Remove `HeroSection` and its min-h-screen if not needed.

2. **Edit `resources/js/components/sections/HeroSection.tsx`:**
   - Remove the `min-h-screen` and `pb-8` from the <section> (or reduce it to just a small wrapper around SearchWidget).
   Alternatively, rename the whole thing to something else and just position SearchWidget.

3. **Edit `resources/js/components/sections/PageHeroCarousel.tsx`:**
   - Add `import { SearchWidget } from '@/components/ui/SearchWidget';`
   - Inside the `relative w-full overflow-hidden` container at the bottom, add an absolute positioned wrapper containing `<SearchWidget />` e.g. `<div className="absolute bottom-0 left-0 right-0 z-20 px-4 pb-4">` -- but we want the search to START at the last 20px of the carousel. If the search widget should start at last 20px of the carousel, set `bottom-[-20px]` so it partially overlaps? Or is it that the search widget's top edge is the last 20px of the carousel? Yes, the user says "the search must start at the last 20 px of the carosel" meaning the search should begin overlapping from 20px before the bottom. So its top starts at `bottom: 20px`. So the element should be positioned `bottom-[20px]` from the bottom? Wait: "at the end of the carosel not after it... the search must start at the last 20 px of the carosel". This implies the search bar should begin at a 20px-thick band at the very bottom of the carousel, overlapping into it. So positioning with `bottom-0` with `-mb-20` or positioning it `bottom-20px` inside `relative` parent. Use negative margin on the search `className="-mt-20"` if the search widget sits in the hero section below.

Concretely, let's wrap the widget in a div in the hero section and place it `relative -top-[20px]` inside a wrapper. Since parent is relative and has overflow-hidden, absolute positioning inside the carousel container directly is easier.

Proposed changes:
- In `HeroSection.tsx`, remove `min-h-screen` to avoid extra whitespace.
- Reduce the `<section className="relative ...">` to just `className="relative"`. In the container inside, we might want `pt-0` or something minimal.
- In `Index.tsx`, wrap `<PageHeroCarousel>` and `<HeroSection>` within `<div className="relative">` so the hero can be positioned with the top overlap if needed.
- Alternatively, keep `HeroSection` but add a negative top margin `-mt-20` to the section so it overlaps the bottom 20px of the carousel. The container inside the hero will then render the `SearchWidget` starting right at that overlap point.

Let's write the plan file now.