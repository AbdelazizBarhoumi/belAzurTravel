# Plan: Add /partners page, category support, and make it dynamic

## Goal
1. Add `/partners` public page (dynamic, fetches from API)
2. Add `category` field to partners (migration + model + controllers)
3. Add CategoryManager button to AdminPartners header
4. Fix the existing `pages/partners/index.tsx` to use API instead of dead `useAdminStore`

---

## Part 1: Backend — Add category to partners

### 1a. Migration
Create `database/migrations/2026_06_19_000002_add_category_to_partners_table.php`:
- Add `category` column (string, nullable) to `partners` table

### 1b. Model
Update `app/Models/Partner.php`:
- Add `'category'` to `$fillable`

### 1c. Admin Controller
Update `app/Http/Controllers/Api/AdminPartnerController.php`:
- Add `'category' => 'nullable|string'` to validation rules
- Include `'category' => $data['category'] ?? null` in attributes return
- Include `'category' => $item->category` in adminPayload return

### 1d. Public Controller
Update `app/Http/Controllers/Api/PartnerController.php`:
- Add `'category' => $partner->category` to the returned array

---

## Part 2: Frontend — Types & API

### 2a. Types
Update `resources/js/types/admin/index.ts`:
- Add `category: string` to `AdminPartner` interface

### 2b. Partners API
Update `resources/js/api/partners.api.ts`:
- Add `category` to `PartnerItem` interface

---

## Part 3: AdminPartners — Category support

Update `resources/js/pages/admin/AdminPartners.tsx`:
- Import `CategoryManager`, `fetchCategories`, `Settings` icon
- Add `catManagerOpen` state
- Fetch categories with `useQuery({ queryKey: ['admin', 'categories', 'partners'], queryFn: () => fetchCategories('partners') })`
- Add category field to the form (Select dropdown from DB categories)
- Add category to validation (required)
- Add "Manage Categories" button in the header actions (next to add button)
- Add category column to the table
- Add category filter pills in the toolbar
- Render `<CategoryManager type="partners" ... />`

---

## Part 4: Public Partners page — Make dynamic

Rewrite `resources/js/pages/partners/index.tsx`:
- Remove `useAdminStore` import
- Use `useQuery` to fetch partners from API via `fetchPartners()`
- Use `useLanguage` for localized text and translations
- Use `PageShell` from `@/components/layout/PageShell` (correct import path)
- Fetch categories from API for filter pills
- Display partner cards with logo, name, category
- Support search and category filtering

---

## Part 5: Route & Nav config

### 5a. Route
Add to `resources/js/app.tsx`:
```tsx
import Partners from './pages/partners/index';
// ...
<Route path="/partners" element={<NavRouteGuard pageKey="partners"><Partners /></NavRouteGuard>} />
```

### 5b. Nav config
`resources/js/lib/nav-config.ts` — partners already added in previous task (key='partners', href='/partners')
- Update href from `'#'` to `'/partners'`

### 5c. Translations
Add to `resources/js/i18n/translations.ts`:
- `partners.title` — "Our Partners"
- `partners.subtitle` — "World-class brands we collaborate with"
- `partners.noResults` — "No partners match your search"
- `partners.searchPlaceholder` — "Search partners..."
- `partners.all` — "All"

---

## Files to Create
| File | Type |
|------|------|
| `database/migrations/2026_06_19_000002_add_category_to_partners_table.php` | Migration |

## Files to Modify
| File | Change |
|------|--------|
| `app/Models/Partner.php` | Add category to fillable |
| `app/Http/Controllers/Api/AdminPartnerController.php` | Handle category |
| `app/Http/Controllers/Api/PartnerController.php` | Return category |
| `resources/js/types/admin/index.ts` | Add category to AdminPartner |
| `resources/js/api/partners.api.ts` | Add category to PartnerItem |
| `resources/js/pages/admin/AdminPartners.tsx` | CategoryManager, category field, filter |
| `resources/js/pages/partners/index.tsx` | Rewrite to use API |
| `resources/js/app.tsx` | Add /partners route |
| `resources/js/lib/nav-config.ts` | Update partners href |
| `resources/js/i18n/translations.ts` | Add partners page translations |

## Verification
1. Run migration
2. Admin → Partners → see "Manage Categories" button, create categories
3. Add partners with categories
4. Visit `/partners` → see dynamic partner grid with category filters
5. Disable partners in nav settings → page guard redirects to `/`
