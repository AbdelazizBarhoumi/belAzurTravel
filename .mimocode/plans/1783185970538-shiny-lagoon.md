# AdminTravels Form UX Improvements

## Target
`resources/js/pages/admin/AdminTravels.tsx`

## What to change

### 1. Import shadcn components (top of file)
Add `Input`, `Textarea`, `Slider` imports. Add `RATING_OPTIONS` from `@/data/adminSelectOptions`.

### 2. Replace raw HTML with shadcn components

**Lines 399-412** — The mapped loop renders raw `<input>` for name/location/duration. Swap to `<Input>`.

**Lines 430-444** — Raw `<textarea>` for description. Swap to `<Textarea>`.

**Lines 461-471** — Raw `<input type="number">` for price. Swap to `<Input type="number">`.

### 3. Replace rating with Select dropdown

**Lines 487-505** — Currently a number input with min=0 max=5. Replace with `<Select>` using RATING_OPTIONS (0, 0.5, 1, ... 5), matching how AdminTours handles rating.

### 4. Replace numeric range fields with Sliders

**Lines 520-535** — duration_days: number input → `<Slider min={0} max={30} step={1}>` with value display badge.

**Lines 544-567** — duration_nights: number input → `<Slider min={0} max={30} step={1}>` with value display badge.

**Lines 575-598** — max_group: number input → `<Slider min={1} max={50} step={1}>` with value display badge.

## No new packages needed
All components exist: `Input`, `Textarea`, `Slider`, `Select` from `@/components/ui/*`, `RATING_OPTIONS` from `@/data/adminSelectOptions`.

## Verification
Open `http://127.0.0.1:8004/admin/travels`, click Add, confirm sliders/rating select render and save works.
